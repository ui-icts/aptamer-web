#!/bin/sh

set -o nounset
set -e

. {{pkg.svc_config_path}}/dotenv

cd {{pkg.path}}/app

DATABASE_USER=${DB_SUPERUSER-$DATABASE_USER} \
  DATABASE_PASSWORD=${DB_SUPERPASS-$DATABASE_PASSWORD} \
  mix do db:create, db:migrate

