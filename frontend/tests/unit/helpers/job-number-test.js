
import { jobNumber } from 'aptamer/helpers/job-number';
import { module, test } from 'qunit';

module('Unit | Helper | job number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = jobNumber([42]);
  assert.ok(result);
});

