<div class="ui secondary menu">
  {{#each menuItems as |mi|}}
    {{log "Menu Value" mi.value}}
    <a class="item {{if mi.selected 'active'}}" {{action (mut currentFilter) mi.value}}> {{mi.text}} </a>
  {{/each}}
</div>

{{yield currentFilter}}
